import { makeStyles, tokens } from "@fluentui/react-components";

export const useGlobalSearchStyle = makeStyles({
    wrapper: {
        columnGap: "0.2rem",
        display: "flex",
        alignItems: "center"
    },
    clearFilterBtn: {
        alignContent: "center",
        alignItems: "center"
    },

});